#include <iostream>
#include <string.h>

using namespace std;

class Result {
	private:
		int roll_no;
		string name;
		int marks;
	public:
		int strength;
		Result(int r, string n, int m) {
			roll_no = r;
			name = n;
			marks = m;
		}
		void input() {
			for(int i = 0; i <= )
			cout << "Enter your "
		}
};

int main() {
	Result obj;
	int str = obj.strength
	cout << "Enter the strength of the class: ";
	
	for(int i = 0; i < str; i++) {
		
 	}
	Result obj[n];
}